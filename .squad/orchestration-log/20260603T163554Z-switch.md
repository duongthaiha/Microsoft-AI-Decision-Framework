# Agent Orchestration Log: Switch

**Timestamp:** 2026-06-03T16:35:54Z  
**Agent:** Switch (Data Engineer)  
**Task:** Create and seed AI Search index `advisor-project-knowledge`  
**Outcome:** SUCCESS

## Summary

Seeded `advisor-project-knowledge` index with 6 project documents. `GET /sessions/:id/similar-projects` now returns ranked matches (top score 0.97 for NFU insurance case).

## Key Decisions

**Seeding approach:** In-network guarded admin endpoint (Option a)
- Leveraged running Container App (already VNet-integrated)
- Used managed identity (Search Index Data Contributor RBAC)
- No public access required; private endpoint remained locked down

## What Was Done

1. **Fixed hardcoded index name bug** — `AzureAiSearchProjectSearch.ensureIndex()` was ignoring `SEARCH_INDEX` env var
   - Changed: `createOrUpdateIndex(DEFINITION)` → `createOrUpdateIndex({ ...DEFINITION, name: this.options.indexName })`
2. **Added guarded POST `/admin/seed/project-knowledge` endpoint**
   - Guard: `ENABLE_ADMIN_SEED=true` required (403 if missing)
   - Created index + seeded 6 documents in single call
3. **Seeded NFU Mutual guidance** into Cosmos `guidance` container (`org-nfum`)
4. **Verified results:**
   - Similar-projects lookup returned 3 ranked matches
   - Top match: Policy Guidance Assistant (NFU insurance) — 0.97 confidence
   - Index remained private-endpoint-only throughout

## Files Changed

- `agents/advisor/data/src/search/AzureAiSearchProjectSearch.ts` — name override fix
- `agents/advisor/api/src/app.ts` — admin seed route
- `agents/advisor/data/scripts/seed-via-admin-endpoint.ps1` — seeding script
- `agents/advisor/data/docs/seeding.md` — seeding documentation

## Repeatability

```bash
# One-shot seeding script
cd agents/advisor/data
.\scripts\seed-via-admin-endpoint.ps1
```

Or manual workflow:
```bash
az containerapp update --set-env-vars ENABLE_ADMIN_SEED=true
curl -X POST https://<ca-url>/admin/seed/project-knowledge -H "Content-Type: application/json" -d '{}'
az containerapp update --remove-env-vars ENABLE_ADMIN_SEED
```

## Notes

- Endpoint is **idempotent** (safe to run multiple times)
- **Post-seeding state:** ENABLE_ADMIN_SEED removed; endpoint locked down
- **Constraint:** AI Search and Cosmos DB remain private-endpoint-only (no public access)

---

**Decision File:** `.squad/decisions.md` → "Decision: AI Search Seeding Approach — advisor-project-knowledge"
