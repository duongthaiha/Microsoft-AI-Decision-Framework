{
  "name": "advisor-root",
  "version": "0.1.0",
  "private": true,
  "description": "AI Framework Advisor Agent POC — workspace root",
  "workspaces": [
    "shared",
    "api",
    "cli",
    "web"
  ],
  "scripts": {
    "build": "npm run build --workspace=shared && npm run build --workspace=api && npm run build --workspace=cli",
    "build:shared": "npm run build --workspace=shared",
    "typecheck": "npm run typecheck --workspace=shared && npm run typecheck --workspace=api && npm run typecheck --workspace=cli",
    "clean": "npm run clean --workspace=shared --if-present && npm run clean --workspace=api --if-present && npm run clean --workspace=cli --if-present"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
